import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
        wrapperImage: {
            height: 235,
        },
        secondWrapper: {
            height: 500
        }
    })

export default styles